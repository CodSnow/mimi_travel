# 咪咪出行 Docker Compose 部署文档

本文档说明如何在本地使用 `docker compose` 启动完整链路，包括：

- `frontend`：React 静态站点 + Nginx
- `backend`：TypeScript BFF / API Gateway
- `python-service`：FastAPI 领域服务
- `postgres`：Python 主业务数据库

## 1. 当前部署结构

### 1.1 服务职责

- `frontend`
  - 构建 `client` 前端产物
  - 使用 Nginx 提供静态资源
  - 将 `/api/*` 请求反向代理到 `backend:3001`
- `backend`
  - 对前端暴露统一 API
  - 保留本地 `data` 目录中的知识库和 demo 状态数据
  - 调用 `python-service` 内部接口
- `python-service`
  - 启动前自动执行 `alembic upgrade head`
  - 提供匹配、定价、风控、评价聚合、订单快照等内部接口
- `postgres`
  - 为 Python 领域服务提供 PostgreSQL 持久化

### 1.2 默认端口

- 前端：`8809`
- TS BFF：`3001`
- Python 服务：`8800`
- PostgreSQL：`5432`

## 2. 前置条件

- 已安装 Docker
- 已安装 Docker Compose
- 当前命令执行目录为项目根目录

## 3. 关键配置

### 3.1 Python 依赖

Python 服务依赖由 [python-service/requirements.txt](/Users/codsevita/Documents/GitHub/mimi_travel/python-service/requirements.txt) 管理，镜像构建时会自动安装。

### 3.2 数据库配置

`docker-compose.yml` 中已经为本地环境写入默认数据库配置：

```yaml
MIMI_DATABASE_URL: postgresql+psycopg://postgres:postgres@postgres:5432/mimi_python
```

如果你要改库名、用户名或密码，需要同时修改：

- `postgres` 服务的 `POSTGRES_*`
- `python-service` 服务的 `MIMI_DATABASE_URL`

### 3.3 服务间鉴权

TS BFF 与 Python 服务通过同一个内部 token 通信：

```yaml
INTERNAL_API_TOKEN: change-me-local-token
MIMI_INTERNAL_API_TOKEN: change-me-local-token
```

这两个值必须保持一致。

## 4. 启动步骤

### 4.1 准备本地数据目录

项目根目录下的 `data` 会挂载到 `backend` 容器内 `/app/data`，用于保存 demo 状态与知识库数据。

如果目录不存在，先创建：

```bash
mkdir -p data
```

### 4.2 构建并启动

在项目根目录执行：

```bash
docker compose up -d --build
```

启动时会发生这些事情：

1. 构建前端镜像并产出静态资源。
2. 构建 TS BFF 镜像。
3. 构建 Python 镜像并安装依赖。
4. 启动 PostgreSQL。
5. `python-service` 在启动前执行数据库迁移。
6. `backend` 等待 `python-service` 健康后启动。
7. `frontend` 等待 `backend` 健康后启动。

## 5. 检查方式

### 5.1 查看服务状态

```bash
docker compose ps
```

预期四个服务都为 `Up` 或 `healthy`。

### 5.2 查看日志

```bash
docker compose logs --tail=200 postgres
docker compose logs --tail=200 python-service
docker compose logs --tail=200 backend
docker compose logs --tail=200 frontend
```

## 6. 访问方式

- 前端首页：`http://localhost:8809`
- TS BFF 健康检查：`http://localhost:3001/health`
- Python 健康检查：`http://localhost:8800/health`

## 7. 常见联调验证

可以优先验证以下链路：

- 直接访问前端首页，确认 Nginx 正常
- 请求 `GET /api/state`，确认前端代理到 TS BFF 正常
- 请求 `POST /api/matching/caregivers`，确认 TS BFF -> Python 内部链路正常
- 请求 `POST /api/pricing/quote`，确认 Python 服务业务逻辑与 DTO 转换正常

## 8. 停止与清理

停止服务：

```bash
docker compose stop
```

移除容器但保留数据卷：

```bash
docker compose down
```

如果连 PostgreSQL 数据卷一起删除：

```bash
docker compose down -v
```
