# 咪咪出行实施计划

更新时间：2026-04-29

## 实施边界

基于《咪咪出行_Python服务与数据结构草案.md》和《咪咪出行_技术设计稿.md》，本次按“当前仓库可落地的 MVP 全链路”实现，范围包括：

- `shared`：补全核心领域类型与 TS/Python 内部 DTO。
- `python-service`：补全配置、鉴权、模型、迁移、仓储、领域服务、内部接口。
- `server`：重构为模块化 BFF，接入 PythonClient，保留现有政策知识库能力，并新增面向前端的聚合接口。

本次不直接接通外部支付渠道、真实地图服务、第三方回调和线上基础设施；对应位置实现为可扩展接口与本地可验证逻辑，不伪装成已联通第三方。

## 当前计划

| 步骤 | 状态 | 说明 |
| --- | --- | --- |
| 1 | 已完成 | 固化计划、边界与验收口径，已落盘到仓库 |
| 2 | 已完成 | 已补全 `shared` 领域模型，并新增 TS `<->` Python 内部 DTO |
| 3 | 已完成 | 已补全 `python-service` 基础设施、缺失模型与分阶段迁移 |
| 4 | 已完成 | 已实现内部鉴权、仓储、规则与领域服务，并接入内部路由 |
| 5 | 已完成 | 已重构 `server` 为模块化 BFF，并接入 PythonClient 与聚合路由 |
| 6 | 已完成 | 已完成本地构建与语法校验，并记录剩余联调风险 |

## 每步验收标准

### 步骤 1

- 计划文件已创建
- 范围、阶段、状态已明确

### 步骤 2

- `shared` 中新增文档定义的核心类型
- TS 内部 DTO 与 Python schema 保持字段语义一致
- 保留旧前端仍在使用的基础类型，避免直接打断现有页面

### 步骤 3

- `python-service` 模型覆盖文档中的核心表
- Alembic 迁移可表达建表意图
- 服务配置与数据库依赖可启动

### 步骤 4

- 内部接口具备服务间鉴权
- 匹配、评价聚合、定价、风控、订单快照不再返回空壳数据
- 业务逻辑拆到 service/repository/rule 层

### 步骤 5

- `server/src/index.ts` 不再承载全部逻辑
- 新增 PythonClient、路由模块和 DTO 转换
- 保留现有政策知识库接口，同时提供文档中核心聚合接口

### 步骤 6

- 至少完成 TypeScript 构建校验
- 至少完成 Python 语法级校验
- 计划状态更新为最终结果，并记录未完成风险

## 验证结果

- `pnpm --filter ./server run build`：通过
- `pnpm --filter ./client run build`：通过
- `python3 -m compileall ./python-service/app ./python-service/migrations`：通过

## 剩余风险

- 真实 `PostgreSQL` 尚未在当前仓库内启动，因此本次主要验证了迁移文件结构与 Python 语法，未做真实建表演练。
- TS BFF 到 Python 服务的 HTTP 联调代码已实现，但未在本地实际拉起两个服务并跑请求级联调。
- 支付渠道、地图平台、第三方回调仍是可扩展接口，不是已接通状态。

## 2026-05-13 Phase 1 验证结果

- Python compile：通过，`docker compose run --rm python-service python -m compileall app migrations`
- Python tests：通过，`docker compose run --rm python-service python -m pytest tests -q`，26 passed
- Server build：通过，`pnpm --filter ./server run build`
- Client build：通过，`pnpm --filter ./client run build`
- 备注：当前 Phase 在隔离 worktree 分支 `phase-1-python-domain-foundation` 中实现并提交。

## 2026-05-13 Phase 2 验证结果

- Python compile：通过，`docker compose run --rm python-service python -m compileall app migrations`
- Python tests：通过，`docker compose run --rm python-service python -m pytest tests -q`，39 passed
- Server build：通过，`pnpm --filter ./server run build`
- Client build：通过，`pnpm --filter ./client run build`
- 备注：Phase 2 将需求、报价、接受报价、订单生成和订单流转切换到 Python/PostgreSQL 主事实源。

## 2026-05-13 Phase 3 验证结果

- Python image build：通过，`docker compose build python-service`
- Python compile：通过，`docker compose run --rm python-service python -m compileall app migrations`
- Python tests：通过，`docker compose run --rm python-service python -m pytest tests -q`，48 passed
- Server build：通过，`pnpm --filter ./server run build`
- Client build：通过，`pnpm --filter ./client run build`
- 备注：Phase 3 将本地支付、退款、会话消息、已读和位置上报接入 Python/PostgreSQL 主事实源，导航链接继续由 TS BFF 本地生成。

## 2026-05-13 Phase 4 验证结果

- Server build：通过，`pnpm --filter ./server run build`
- Client build：通过，`pnpm --filter ./client run build`
- 备注：Phase 4 修复 H5 对 Python-backed BFF API 的用户上下文传参，新增轻量 `screen + params` 流程页，覆盖需求大厅、服务者详情、订单确认、支付确认、支付结果、订单详情、导航、会话、政策详情、宠物档案、地址、收藏、支付记录、退款、争议、服务者工作台、报价管理和管理后台入口。

## 2026-05-13 Phase 5 验证结果

- Python image build：通过，`docker compose build python-service`
- Python compile：通过，`docker compose run --rm python-service python -m compileall app migrations`
- Python tests：通过，`docker compose run --rm python-service python -m pytest tests -q`，54 passed
- Server build：通过，`pnpm --filter ./server run build`
- Client build：通过，`pnpm --filter ./client run build`
- 备注：Phase 5 补齐治理、投诉争议、政策收藏、材料 checklist、本地 RAG 和 DeepSeek/provider 边界。

## 2026-05-13 Phase 6 验证结果

- Python image build：通过，`docker compose build python-service`
- Python compile：通过，`docker compose run --rm python-service python -m compileall app migrations`
- Python tests：通过，`docker compose run --rm python-service python -m pytest tests -q`，56 passed
- Server build：通过，`pnpm --filter ./server run build`
- Client build：通过，`pnpm --filter ./client run build`
- Docker Compose：通过，`docker compose up -d --build`，PostgreSQL、Python、Backend、Frontend 均 healthy
- Compose smoke：通过，`node scripts/compose-smoke.mjs`，覆盖 `/api/state`、`/api/knowledge`、`/api/ask` 兼容接口，以及登录、服务者、需求、报价、接单、支付、会话主链路
- 备注：Phase 6 修复 Compose 空库端到端断裂问题，将 BFF 登录和服务者读取接入 Python/PostgreSQL，固定 Docker pnpm 版本，并增加可重复冒烟守卫。

## 2026-05-14 Phase 7 验证结果

- 展示资源边界：通过，Phase 7 未改首页轮播文件，轮播继续展示 `banner-suitcase-cat.svg` 和 `banner-round-cat.svg` 图片；仅将缺失 demo avatar URL 替换为相近 emoji。
- Server build：通过，`pnpm --filter ./server run build`
- Client build：通过，`pnpm --filter ./client run build`
- Python image build：通过，`docker compose build python-service`
- Python compile：通过，`docker compose run --rm python-service python -m compileall app migrations tests`
- Python tests：通过，`docker compose run --rm python-service python -m pytest tests -q`，62 passed
- 代码审查修复：刷新恢复登录时先恢复 `currentUserId` 再加载用户态数据；评价 reviewer 只能来自 operator；照护反馈要求订单 seller operator；重复评价并发冲突映射为 409。
- 备注：Phase 7 将宠物档案、常用地址、服务者入驻申请、评价和照护反馈接入 Python/PostgreSQL；H5 辅助页已展示并创建宠物/地址/反馈真实数据。

## 2026-05-15 需求审查后的未完成范围

- 服务者档期：仍需独立 `provider_schedules` 表、维护 API 和 H5 服务者工作台入口。
- 宠物档案：仍需补齐体型、默认携带方式等字段，并进入下单快照。
- 支付闭环：当前 H5 主要跑通订金支付；全额、订金、尾款三种支付场景都需要可验收。
- 退款闭环：当前退款页偏争议入口；需要真实退款申请、退款单、支付退款和退款查询闭环。
- 管理后台：需要补齐审核操作、投诉/争议处理、订单/退款明细、下架异常服务者和审计查看。
- 服务者工作台：需要补齐服务者身份下的可接需求、主动报价、接单、服务反馈和状态管理。
- 办证/托运协助：需要补齐材料、节点、状态跟踪和陪同确认，不只停留在泛化服务类型。
- 真实三方支付：真实支付宝和微信支付接入列为未完成项，需要商户配置、签名验签、预下单、查询、回调、关闭、退款、退款查询、幂等和重放防护。
- 真实 DeepSeek：真实 DeepSeek API 接入列为未完成项，需要配置管理、超时、重试、降级、本地 RAG 引用融合、日志脱敏和敏感信息过滤。
- 真实地图 SDK：真实高德/百度 SDK 接入列为未完成项，需要定位授权、路线规划、导航唤起、Web fallback 和无权限降级。
- 小程序完整功能对齐：当前只保持演示兼容；完整登录、需求、报价、订单、支付、消息、政策、宠物档案、地址、退款、投诉、服务者工作台和必要管理能力均列为未完成项。

## 2026-05-15 Phase 8 验证结果

- Server build：通过，`pnpm --filter ./server run build`
- Client build：通过，`pnpm --filter ./client run build`
- Client typecheck：通过，`pnpm --filter ./client exec tsc --noEmit --pretty false`
- Python compile：通过，`docker compose run --rm --no-deps python-service python -m compileall app/api/internal/payments.py app/services/payments app/schemas/payments.py tests/services/test_payment_service.py`
- Python payment tests：通过，`docker compose run --rm --no-deps -e MIMI_TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@host.docker.internal:54322/mimi_python python-service python -m pytest tests/services/test_payment_provider.py tests/services/test_payment_service.py tests/api/test_payment_message_location_api.py -q`，10 passed
- Python tests：通过，`docker compose run --rm --no-deps -e MIMI_TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@host.docker.internal:54322/mimi_python python-service python -m pytest tests -q`，62 passed
- MP H5 build：通过，`pnpm --filter ./mp run build:h5`，仍有 Taro/React 19 兼容警告和 bundle size 警告
- MP WeApp build：通过，`pnpm --filter ./mp run build:weapp`，仍有 Taro webpack 预编译 schema 警告
- Diff check：通过，`git diff --check`
- 备注：Phase 8 已接入真实 DeepSeek HTTP provider、地图 SDK 配置和 H5 SDK 导航、小程序通过 BFF API 的最小完整入口、支付真实 provider 配置边界、退款查询 API、支付通知验签结果消费和回调不变量校验。真实支付宝/微信网关的预下单、查询、退款、退款查询、签名验签 HTTP 适配仍需要真实商户配置、证书/私钥和对应签名适配后继续完成，当前不会伪造真实成功。
