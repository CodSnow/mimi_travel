# 咪咪出行实施计划

更新时间：2026-04-29

## 实施边界

基于《咪咪出行_Python服务与数据结构草案.md》和《咪咪出行_技术设计稿.md》，本次按“当前仓库可落地的 MVP 全链路”实现，范围包括：

- `shared`：补全核心领域类型与 TS/Python 内部 DTO。
- `python-service`：补全配置、鉴权、模型、迁移、仓储、领域服务、内部接口。
- `server`：重构为模块化 BFF，接入 PythonClient，保留现有政策知识库能力，并新增面向前端的聚合接口。

本次不直接接通外部支付渠道、真实地图服务、第三方回调和线上基础设施；对应位置实现为可扩展接口与本地可验证逻辑，不伪装成已联通第三方。

## 当前计划

| 步骤 | 状态 | 说明 |
| --- | --- | --- |
| 1 | 已完成 | 固化计划、边界与验收口径，已落盘到仓库 |
| 2 | 已完成 | 已补全 `shared` 领域模型，并新增 TS `<->` Python 内部 DTO |
| 3 | 已完成 | 已补全 `python-service` 基础设施、缺失模型与分阶段迁移 |
| 4 | 已完成 | 已实现内部鉴权、仓储、规则与领域服务，并接入内部路由 |
| 5 | 已完成 | 已重构 `server` 为模块化 BFF，并接入 PythonClient 与聚合路由 |
| 6 | 已完成 | 已完成本地构建与语法校验，并记录剩余联调风险 |

## 每步验收标准

### 步骤 1

- 计划文件已创建
- 范围、阶段、状态已明确

### 步骤 2

- `shared` 中新增文档定义的核心类型
- TS 内部 DTO 与 Python schema 保持字段语义一致
- 保留旧前端仍在使用的基础类型，避免直接打断现有页面

### 步骤 3

- `python-service` 模型覆盖文档中的核心表
- Alembic 迁移可表达建表意图
- 服务配置与数据库依赖可启动

### 步骤 4

- 内部接口具备服务间鉴权
- 匹配、评价聚合、定价、风控、订单快照不再返回空壳数据
- 业务逻辑拆到 service/repository/rule 层

### 步骤 5

- `server/src/index.ts` 不再承载全部逻辑
- 新增 PythonClient、路由模块和 DTO 转换
- 保留现有政策知识库接口，同时提供文档中核心聚合接口

### 步骤 6

- 至少完成 TypeScript 构建校验
- 至少完成 Python 语法级校验
- 计划状态更新为最终结果，并记录未完成风险

## 验证结果

- `pnpm --filter ./server run build`：通过
- `pnpm --filter ./client run build`：通过
- `python3 -m compileall ./python-service/app ./python-service/migrations`：通过

## 剩余风险

- 真实 `PostgreSQL` 尚未在当前仓库内启动，因此本次主要验证了迁移文件结构与 Python 语法，未做真实建表演练。
- TS BFF 到 Python 服务的 HTTP 联调代码已实现，但未在本地实际拉起两个服务并跑请求级联调。
- 支付渠道、地图平台、第三方回调仍是可扩展接口，不是已接通状态。

## 2026-05-13 Phase 1 验证结果

- Python compile：通过，`docker compose run --rm python-service python -m compileall app migrations`
- Python tests：通过，`docker compose run --rm python-service python -m pytest tests -q`，26 passed
- Server build：通过，`pnpm --filter ./server run build`
- Client build：通过，`pnpm --filter ./client run build`
- 备注：当前 Phase 在隔离 worktree 分支 `phase-1-python-domain-foundation` 中实现并提交。

## 2026-05-13 Phase 2 验证结果

- Python compile：通过，`docker compose run --rm python-service python -m compileall app migrations`
- Python tests：通过，`docker compose run --rm python-service python -m pytest tests -q`，39 passed
- Server build：通过，`pnpm --filter ./server run build`
- Client build：通过，`pnpm --filter ./client run build`
- 备注：Phase 2 将需求、报价、接受报价、订单生成和订单流转切换到 Python/PostgreSQL 主事实源。

## 2026-05-13 Phase 3 验证结果

- Python image build：通过，`docker compose build python-service`
- Python compile：通过，`docker compose run --rm python-service python -m compileall app migrations`
- Python tests：通过，`docker compose run --rm python-service python -m pytest tests -q`，48 passed
- Server build：通过，`pnpm --filter ./server run build`
- Client build：通过，`pnpm --filter ./client run build`
- 备注：Phase 3 将本地支付、退款、会话消息、已读和位置上报接入 Python/PostgreSQL 主事实源，导航链接继续由 TS BFF 本地生成。

## 2026-05-13 Phase 4 验证结果

- Server build：通过，`pnpm --filter ./server run build`
- Client build：通过，`pnpm --filter ./client run build`
- 备注：Phase 4 修复 H5 对 Python-backed BFF API 的用户上下文传参，新增轻量 `screen + params` 流程页，覆盖需求大厅、服务者详情、订单确认、支付确认、支付结果、订单详情、导航、会话、政策详情、宠物档案、地址、收藏、支付记录、退款、争议、服务者工作台、报价管理和管理后台入口。
