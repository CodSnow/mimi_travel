# 咪咪出行技术设计稿

## 1. 文档目标

本文档用于定义咪咪出行围绕“发布需求、附近匹配、消息、订单、支付、定位导航”的技术落地方案，重点覆盖：

- 系统边界
- 核心数据模型
- 后端模块拆分
- API 设计
- 支付接入方案
- 地图导航方案
- 持久化方案
- 实施路径

本文档默认基于当前仓库结构：

- `client`：React Web 客户端
- `mp`：小程序端
- `server`：Express 后端
- `python-service`：新增 Python 领域服务
- `shared`：前后端共享类型

## 2. 当前现状

当前后端能力较轻：

- `GET /api/state`
- `PUT /api/state`
- `GET /api/knowledge`
- `GET /api/knowledge/:id`
- `POST /api/ask`

当前持久化方式为本地 JSON 文件，适合 demo，不适合支付、订单和消息场景。涉及交易和支付后，需要升级为数据库持久化，并增加幂等、验签、审计和状态机能力。

当前最合理的演进方向不是直接用 TypeScript 后端承接所有复杂逻辑，而是在保留现有 TS 架构的前提下，再增加一层 Python 后端，用于处理：

- 推荐排序
- 画像聚合
- 复杂规则判断
- 风控
- 动态定价
- 敏感数据逻辑
- 主业务结构存储

## 3. 总体架构

### 3.1 逻辑架构

- 前端层
  - Web SPA
  - 小程序端
- TS 接入与编排层
  - Express API
  - BFF / API Gateway
  - 鉴权
  - DTO 转换
  - 聚合编排
  - 第三方回调入口
- Python 领域服务层
  - Matching Engine
  - Care Recommendation Engine
  - Ride Recommendation Engine
  - Review Aggregation Engine
  - Risk Engine
  - Pricing Engine
  - Sensitive Data Logic
- 基础设施层
  - PostgreSQL
  - Redis
  - 对象存储
  - 第三方支付渠道
  - 地图跳转渠道

### 3.2 分层职责

#### TypeScript 后端职责

- 作为前端唯一后端入口。
- 负责登录态、鉴权、限流、参数校验。
- 聚合前端页面所需数据。
- 负责 TS DTO 和 Python DTO 的相互转换。
- 屏蔽 Python 领域服务实现细节。
- 承担支付回调、地图跳转、消息入口等对外接口职责。

#### Python 后端职责

- 负责复杂领域逻辑。
- 负责需求、报价、订单、评价、画像等复杂结构的主存储。
- 负责推荐排序、评分聚合、策略计算、敏感数据处理。
- 输出稳定、可版本化的内部领域接口。

#### 前端职责

- 只调用 TypeScript 后端。
- 不直接感知 Python 层存在。

### 3.3 服务间交互方式

建议分阶段演进：

- 第一阶段：TS 调用 Python 走内部 HTTP JSON API。
- 第二阶段：高频核心能力可升级为 gRPC。
- 异步任务可通过 Redis Stream、RabbitMQ 或 Kafka 解耦。

适合同步调用的场景：

- 发布需求后的候选服务者计算
- 司机 / 闲人画像聚合
- 下单前规则校验
- 支付前风控判断

适合异步调用的场景：

- 评价聚合重算
- 画像刷新
- 风控规则训练
- 报表和统计

### 3.4 推荐技术选型

- TS 接入层：Node.js + Express 5 + TypeScript
- Python 领域层：FastAPI
- TS ORM：Prisma 或 Drizzle
- Python ORM：SQLAlchemy 或 SQLModel
- 主数据库：PostgreSQL
- 缓存与实时状态：Redis
- 实时消息：WebSocket 或 SSE
- TS 校验：Zod
- Python 校验：Pydantic
- TS 日志：Pino
- Python 日志：structlog 或标准 JSON 日志
- TS 任务调度：BullMQ
- Python 异步任务：Celery 或 RQ

### 3.5 数据库选型评估

当前项目更推荐 PostgreSQL，而不是其他 NoSQL 作为主数据库，原因如下：

- 订单、支付、退款、评价和消息都属于强关系数据。
- 需要事务一致性，特别是支付和订单流转。
- 需要复杂查询、排序和聚合。
- 需要保留结构化模型，同时兼容灵活字段。
- PostgreSQL 可通过 JSONB 保存灵活的需求参数和规则快照。
- 若后续地理能力复杂，可进一步引入 PostGIS。

评估结论：

- PostgreSQL：最适合作为 Python 领域层主数据库。
- Redis：适合作为缓存、锁、延迟任务和短时状态层。
- 对象存储：适合服务反馈图片、视频、附件。

不建议当前阶段优先选择纯文档型数据库作为主库，因为交易、支付、评价和订单更需要关系型一致性。

## 4. 核心领域模型

建议在 `shared` 中新增以下类型。

### 4.1 用户与服务者

```ts
export interface UserProfile {
  id: string;
  nickname: string;
  phone: string;
  avatar: string;
  role: 'customer' | 'provider' | 'admin';
  verified: boolean;
  createdAt: string;
}

export interface ProviderProfile {
  userId: string;
  status: 'pending' | 'approved' | 'rejected';
  services: ServiceType[];
  intro: string;
  serviceRadiusKm: number;
  baseDistrict: string;
  score: number;
  completedOrderCount: number;
  catCareScore?: number;
  communicationScore?: number;
  punctualityScore?: number;
  emergencyHandlingScore?: number;
  petFriendlyScore?: number;
  drivingStabilityScore?: number;
  cleanlinessScore?: number;
  supportsHomeVisit?: boolean;
  supportsMedication?: boolean;
  supportsMultiDayCare?: boolean;
  supportsEmergencyOrder?: boolean;
  catCareTags?: string[];
}
```

建议为出行服务者补充车辆与宠物友好信息：

```ts
export interface VehicleProfile {
  id: string;
  userId: string;
  vehicleType: 'economy' | 'comfort' | 'suv' | 'business_van';
  plateMasked: string;
  seats: number;
  trunkLevel: 'small' | 'medium' | 'large';
  supportsCatBag: boolean;
  supportsCrate: boolean;
  supportsStroller: boolean;
  supportsMultiPet: boolean;
  petFriendly: boolean;
  petFriendlyTags: string[];
}
```

### 4.2 位置

```ts
export interface LocationPoint {
  lat: number;
  lng: number;
  address: string;
  district?: string;
  adcode?: string;
  coordSystem?: 'gcj02' | 'wgs84' | 'bd09ll';
}
```

### 4.3 需求单

```ts
export type ServiceType =
  | 'buddy'
  | 'feeding'
  | 'cleaning'
  | 'playtime'
  | 'temporary_care'
  | 'hospital'
  | 'medication'
  | 'multi_day_care'
  | 'grooming_pickup'
  | 'taxi'
  | 'ride'
  | 'pet_friendly_taxi'
  | 'carpool'
  | 'escort'
  | 'shipping_assist';

export interface Demand {
  id: string;
  userId: string;
  serviceType: ServiceType;
  title: string;
  description: string;
  petSummary: string;
  budgetMin: number;
  budgetMax: number;
  expectedPrice?: number;
  pickup?: LocationPoint;
  destination?: LocationPoint;
  serviceTime: string;
  contactName: string;
  contactPhone: string;
  allowBargain: boolean;
  visibilityRadiusKm: number;
  careRequirements?: CareRequirements;
  rideRequirements?: RideRequirements;
  status: 'open' | 'matched' | 'paid' | 'in_service' | 'completed' | 'cancelled' | 'refunding';
  selectedOfferId?: string;
  createdAt: string;
  updatedAt: string;
}

export interface CareRequirements {
  needHomeVisit?: boolean;
  needCleaning?: boolean;
  needMedication?: boolean;
  needPhotoFeedback?: boolean;
  needVideoFeedback?: boolean;
  needMultiDayCare?: boolean;
  visitTimesPerDay?: number;
  estimatedDurationMinutes?: number;
  hasMultiplePets?: boolean;
  caregiverPreference?: 'any' | 'male' | 'female';
  requireCatCareExperience?: boolean;
}

export interface RideRequirements {
  petCount?: number;
  petSize?: 'small' | 'medium' | 'large';
  carrierType?: 'none' | 'cat_bag' | 'crate' | 'stroller';
  acceptNormalTaxi?: boolean;
  requirePetFriendlyVehicle?: boolean;
  requireLargeTrunk?: boolean;
  requireStableDriving?: boolean;
  requireLowOdor?: boolean;
}
```

### 4.4 报价单

```ts
export interface Offer {
  id: string;
  demandId: string;
  providerUserId: string;
  quoteAmountFen: number;
  message?: string;
  etaMinutes?: number;
  vehicleId?: string;
  servicePlan?: string;
  status: 'submitted' | 'accepted' | 'rejected' | 'withdrawn';
  createdAt: string;
}
```

### 4.5 订单

```ts
export interface Order {
  id: string;
  demandId: string;
  buyerUserId: string;
  sellerUserId: string;
  title: string;
  amountFen: number;
  depositFen?: number;
  status:
    | 'pending_payment'
    | 'paid'
    | 'confirmed'
    | 'arriving'
    | 'serving'
    | 'completed'
    | 'cancelled'
    | 'refund_pending'
    | 'refunded';
  serviceTime: string;
  pickup?: LocationPoint;
  destination?: LocationPoint;
  vehicleId?: string;
  driverSnapshot?: DriverSnapshot;
  caregiverSnapshot?: CaregiverSnapshot;
  createdAt: string;
  updatedAt: string;
}

export interface DriverSnapshot {
  userId: string;
  nickname: string;
  avatar: string;
  petFriendlyScore?: number;
  tags: string[];
  vehicleType?: string;
}

export interface CaregiverSnapshot {
  userId: string;
  nickname: string;
  avatar: string;
  catCareScore?: number;
  tags: string[];
  supportsMedication?: boolean;
  supportsMultiDayCare?: boolean;
}
```

### 4.6 支付

```ts
export interface Payment {
  id: string;
  orderId: string;
  channel: 'alipay' | 'wechat_pay';
  scene: 'deposit' | 'full' | 'balance';
  amountFen: number;
  currency: 'CNY';
  status: 'created' | 'pending' | 'paid' | 'failed' | 'closed' | 'refunded' | 'partial_refunded';
  outTradeNo: string;
  providerTradeNo?: string;
  rawNotify?: string;
  paidAt?: string;
  createdAt: string;
  updatedAt: string;
}
```

### 4.7 消息

```ts
export interface Conversation {
  id: string;
  demandId?: string;
  orderId?: string;
  participantUserIds: string[];
  lastMessageAt: string;
}

export interface Message {
  id: string;
  conversationId: string;
  senderUserId: string;
  type: 'text' | 'system' | 'offer' | 'payment' | 'location';
  content: string;
  relatedDemandId?: string;
  createdAt: string;
}
```

### 4.8 评价

宠物出行类订单建议使用多维评分，而不是只有单一星级。

```ts
export interface Review {
  id: string;
  orderId: string;
  reviewerUserId: string;
  revieweeUserId: string;
  overallScore: number;
  catCareScore?: number;
  petFriendlyScore?: number;
  drivingStabilityScore?: number;
  punctualityScore?: number;
  cleanlinessScore?: number;
  communicationScore?: number;
  feedbackCompletenessScore?: number;
  medicationAccuracyScore?: number;
  supportsPetHandlingScore?: number;
  tags: string[];
  content?: string;
  createdAt: string;
}

export interface ServiceFeedback {
  id: string;
  orderId: string;
  providerUserId: string;
  arrivedAt?: string;
  leftAt?: string;
  note?: string;
  photoUrls: string[];
  videoUrls: string[];
  createdAt: string;
}
```

## 5. 状态机设计

### 5.1 需求单状态

- `open`：已发布，等待报名或报价
- `matched`：已选中服务者
- `paid`：已支付
- `in_service`：服务中
- `completed`：已完成
- `cancelled`：已取消
- `refunding`：退款处理中

### 5.2 订单状态

- `pending_payment`
- `paid`
- `confirmed`
- `arriving`
- `serving`
- `completed`
- `cancelled`
- `refund_pending`
- `refunded`

### 5.3 支付状态

- `created`
- `pending`
- `paid`
- `failed`
- `closed`
- `refunded`
- `partial_refunded`

## 6. 模块拆分

### 6.1 `server/src/modules/auth`

- 登录
- 登出
- 当前用户
- 后续扩展 JWT / Session

### 6.2 `server/src/modules/users`

- 用户资料
- 服务者资料
- 入驻审核
- 闲人照护能力配置

### 6.3 `server/src/modules/demands`

- 发布需求
- 需求列表
- 需求详情
- 取消需求
- 需求搜索
- 出行条件筛选
- 照护条件筛选

### 6.4 `server/src/modules/offers`

- 报价
- 撤回报价
- 接受报价
- 拒绝报价
- 提交照护方案

### 6.5 `server/src/modules/orders`

- 创建订单
- 查询订单
- 状态推进
- 完成确认
- 取消订单
- 出行司机快照固化
- 照护闲人快照固化
- 服务反馈记录

### 6.6 `server/src/modules/payments`

- 创建支付单
- 发起支付
- 处理回调
- 交易查询
- 关闭支付
- 退款

### 6.7 `server/src/modules/messages`

- 会话列表
- 消息列表
- 发消息
- 已读状态
- 系统消息分发

### 6.8 `server/src/modules/reviews`

- 提交评价
- 查询司机评价聚合
- 查询标签分布
- 生成司机多维评分画像
- 生成闲人照护评分画像

### 6.9 `server/src/modules/navigation`

- 位置上报
- 生成地图跳转链接
- 附近服务者查询
- 出行司机排序支持

### 6.10 `server/src/modules/policy`

- 保留现有知识库
- 后续增加管理能力

### 6.11 `server` 作为 TS 接入层新增职责

- 统一前端 API 协议。
- 封装对 Python 服务的内部调用。
- 统一错误码和用户可读文案。
- 做前端视图级聚合。

### 6.12 `python-service/app/modules`

建议 Python 领域服务拆为：

- `matching`
- `care`
- `ride`
- `reviews`
- `pricing`
- `risk`
- `profiles`
- `orders_core`

## 7. API 设计

### 7.1 认证

```http
POST /api/auth/login
POST /api/auth/logout
GET /api/auth/me
```

### 7.2 用户与服务者

```http
GET /api/users/me
PUT /api/users/me
GET /api/providers/:userId
POST /api/providers/apply
PUT /api/providers/me
```

### 7.3 需求单

```http
POST /api/demands
GET /api/demands
GET /api/demands/:id
POST /api/demands/:id/cancel
```

查询参数建议：

- `serviceType`
- `district`
- `lat`
- `lng`
- `radiusKm`
- `petFriendly`
- `supportsCrate`
- `supportsMultiPet`
- `vehicleType`
- `supportsMedication`
- `supportsHomeVisit`
- `supportsMultiDayCare`
- `page`
- `pageSize`

### 7.4 报价

```http
POST /api/demands/:id/offers
GET /api/demands/:id/offers
POST /api/offers/:id/accept
POST /api/offers/:id/reject
POST /api/offers/:id/withdraw
```

### 7.4.1 司机评价

```http
POST /api/orders/:id/reviews
GET /api/providers/:userId/reviews
GET /api/providers/:userId/review-summary
```

### 7.4.2 照护反馈

```http
POST /api/orders/:id/feedback
GET /api/orders/:id/feedback
```

### 7.5 订单

```http
GET /api/orders
GET /api/orders/:id
POST /api/orders/:id/confirm-arrival
POST /api/orders/:id/start-service
POST /api/orders/:id/complete
POST /api/orders/:id/cancel
```

### 7.6 支付

```http
POST /api/payments
GET /api/payments/:id
POST /api/payments/:id/query
POST /api/payments/:id/refund
POST /api/payments/notify/alipay
POST /api/payments/notify/wechat
```

创建支付单请求示例：

```json
{
  "orderId": "ord_xxx",
  "channel": "alipay",
  "scene": "deposit"
}
```

返回示例：

```json
{
  "paymentId": "pay_xxx",
  "status": "pending",
  "outTradeNo": "MIMI202604290001",
  "channelPayload": {
    "payUrl": "https://...",
    "appParams": {}
  }
}
```

### 7.7 消息

```http
GET /api/messages/conversations
GET /api/messages/conversations/:id
POST /api/messages/conversations/:id
POST /api/messages/read
```

### 7.8 导航与定位

```http
POST /api/locations/report
GET /api/locations/nearby-providers
GET /api/navigation/link
```

导航接口建议参数：

- `fromLat`
- `fromLng`
- `toLat`
- `toLng`
- `toName`
- `mode=driving|walking`

返回：

```json
{
  "amap": "androidamap://route?...",
  "baidu": "baidumap://map/direction?...",
  "webFallback": "https://uri.amap.com/navigation?... "
}
```

### 7.9 TS 与 Python 内部接口

以下接口仅供 TS 后端内部调用，不对前端开放：

```http
POST /internal/matching/caregivers
POST /internal/matching/drivers
POST /internal/reviews/provider-summary
POST /internal/risk/prepay-check
POST /internal/pricing/quote
POST /internal/orders/snapshot
```

内部接口要求：

- 使用服务间鉴权。
- 使用稳定 DTO。
- 关键写操作带幂等键。
- 返回领域结果而不是底层表结构。

## 8. 数据库设计

### 8.1 推荐持久化方案

- PostgreSQL：Python 领域层主业务数据
- Redis：在线态、幂等锁、附近用户缓存、延迟任务
- TS 层不再以 JSON FS 作为主存储

### 8.2 TS 层与 Python 层数据边界

建议以 Python 领域数据库为主事实来源。

Python 侧主存储：

- 需求
- 报价
- 订单
- 支付
- 评价
- 服务者画像
- 推荐结果快照
- 风控判定记录

TS 侧可保留：

- 轻量缓存
- 聚合结果缓存
- 回调幂等缓存
- 临时展示态

### 8.3 核心表

- `users`
- `provider_profiles`
- `vehicle_profiles`
- `pets`
- `demands`
- `offers`
- `orders`
- `payments`
- `refunds`
- `conversations`
- `messages`
- `reviews`
- `service_feedbacks`
- `location_snapshots`
- `order_events`

### 8.4 关键表说明

`payments`：

- 保存每次支付发起记录
- 唯一约束 `out_trade_no`
- 存储第三方交易号
- 存储回调原文摘要

`order_events`：

- 保存订单状态变更历史
- 用于审计和故障排查

`location_snapshots`：

- 只存必要的订单相关位置
- 不做无限期精细轨迹留存

`reviews`：

- 存储订单完成后的双向评价
- 支持多维评分与标签
- 为司机排序、宠物友好推荐和闲人照护推荐提供统计基础

`service_feedbacks`：

- 存储上门服务过程中的图文反馈
- 用于订单复盘、投诉处理和服务留痕

### 8.5 选型结论

基于当前项目评估：

- 主数据库优先选择 PostgreSQL
- Python 领域层直接连接 PostgreSQL
- TypeScript 层通过 Python 服务访问主业务事实数据
- 如果后期有大量分析报表，再单独评估分析型数据库

## 9. 支付设计

### 9.1 设计原则

- 订单和支付解耦
- 支付渠道可插拔
- 所有支付结果以后端异步通知为准
- 前端支付成功返回只作为体验提示，不作为最终入账依据
- 所有支付和退款均需幂等

### 9.2 Provider 抽象

```ts
export interface PaymentProvider {
  createPayment(input: CreatePaymentInput): Promise<CreatePaymentResult>;
  queryPayment(outTradeNo: string): Promise<PaymentQueryResult>;
  closePayment(outTradeNo: string): Promise<void>;
  refundPayment(input: RefundPaymentInput): Promise<RefundResult>;
  verifyNotify(rawBody: string, headers?: Record<string, string>): Promise<VerifyResult>;
}
```

### 9.3 支付宝接入方案

首期建议以支付宝为首个正式渠道。

接入步骤：

1. 在支付宝开放平台创建应用。
2. 开通对应支付能力。
3. 配置应用公钥、私钥、网关和回调地址。
4. 先使用沙箱环境联调。
5. 后端通过 `PaymentService` 发起支付。
6. 前端拉起支付宝收银台或跳转支付页面。
7. 支付宝异步回调 `notify_url`。
8. 后端验签、校验金额、校验订单、幂等更新支付状态。
9. 更新订单状态并发送系统消息。

### 9.4 支付创建流程

1. 校验订单状态必须为 `pending_payment`。
2. 创建本地 `payments` 记录，状态为 `created`。
3. 调用 `AlipayProvider.createPayment`。
4. 更新本地支付状态为 `pending`。
5. 返回支付参数给前端。

### 9.5 支付回调流程

1. 接收支付宝通知。
2. 使用支付宝公钥验签。
3. 校验 `outTradeNo` 是否存在。
4. 校验金额是否与本地一致。
5. 校验商户和应用标识。
6. 若支付已处理过，则直接返回成功，保证幂等。
7. 更新 `payments.status = paid`。
8. 更新 `orders.status = paid`。
9. 记录 `order_events`。
10. 写入系统消息。

### 9.6 主动补单

必须保留主动查询能力，防止：

- 前端已支付，但回调丢失
- 回调延迟
- 用户提前关闭支付页

实现方式：

- 提供 `POST /api/payments/:id/query`
- 对 `pending` 支付加入延迟任务自动补查

### 9.7 退款

退款必须走后端：

1. 校验订单是否允许退款。
2. 生成退款记录。
3. 调用 `refundPayment`。
4. 更新支付和订单状态。
5. 发送退款结果消息。

## 10. 定位与导航设计

### 10.1 坐标统一

建议统一使用 `gcj02` 作为国内地图坐标存储标准。若前端来源不同，由导航模块进行转换适配。

### 10.2 位置使用场景

- 发布需求时选择上门点和目的地。
- 服务者更新当前位置。
- 订单页计算距离和预计到达时间。
- 地图跳转。

对于同城接送，还应支持：

- 司机到用户上车点距离估算
- 用户步行到约定上车点导航
- 医院、机场、火车站等固定场景点位跳转

### 10.3 第三方跳转

首期不强依赖原生地图 SDK，先做跳转链接生成。

支持：

- 高德地图 URI
- 百度地图 URI

### 10.4 导航服务接口

```ts
interface NavigationLink {
  amap?: string;
  baidu?: string;
  webFallback?: string;
}
```

后端负责：

- 参数校验
- 坐标格式处理
- URI 生成

前端负责：

- 展示跳转入口
- 调起浏览器或客户端

## 11. 消息系统设计

### 11.1 消息类型

- `text`
- `system`
- `offer`
- `payment`
- `location`
- `service_feedback`

### 11.2 会话建立规则

- 用户发布需求后，系统可建立需求会话。
- 服务者报价后，用户与该服务者形成私有会话。
- 订单成交后，会话升级为订单会话。

### 11.3 推送方式

首期可采用：

- 轮询获取会话列表和消息

二期可升级：

- WebSocket
- SSE

## 12. 摇人陪咪推荐设计

### 12.1 目标

照护类需求推荐应优先返回“更可靠、更匹配、更有照护经验”的闲人。

### 12.2 排序因子

建议构建综合分：

- 距离分
- 档期匹配分
- 照护能力匹配分
- 猫咪照护评分
- 守时评分
- 沟通评分
- 反馈完整度评分
- 喂药能力匹配分
- 多猫经验分
- 成交价格分

### 12.3 硬过滤条件

- 用户要求上门，但闲人未开启上门服务
- 用户要求喂药，但闲人不支持
- 用户要求连续多日，但闲人档期不满足
- 用户要求真实养猫经验，但闲人资料不满足

### 12.4 展示数据

前端推荐卡片建议展示：

- 总分
- 养猫经验
- 历史照护订单数
- 守时分
- 照护细致度分
- 喂药能力标签
- 多猫家庭经验标签

## 13. 宠物友好司机推荐设计

### 13.1 目标

对宠物出行类需求，不只返回“最近或最便宜”的司机，而是返回“最适合宠物”的司机。

### 13.2 排序因子

建议构建综合分：

- 距离分
- ETA 分
- 宠物友好分
- 驾驶平稳分
- 卫生整洁分
- 守时分
- 历史宠物订单量
- 是否满足用户硬性条件
- 报价竞争力

可表示为：

```ts
score =
  w1 * distanceScore +
  w2 * etaScore +
  w3 * petFriendlyScore +
  w4 * drivingStabilityScore +
  w5 * cleanlinessScore +
  w6 * punctualityScore +
  w7 * experienceScore +
  w8 * fitRequirementScore +
  w9 * priceScore
```

### 13.3 硬过滤条件

在排序前先过滤：

- 用户要求宠物友好车，但司机未开启宠物友好
- 用户要求支持猫包/航空箱，但车辆不支持
- 用户要求多宠同行，但司机不支持
- 用户要求大空间，但车型不满足

### 13.4 展示数据

前端推荐卡片建议展示：

- 总分
- 宠物友好标签
- 历史宠物订单数
- 宠物友好评分
- 驾驶平稳评分
- 近 30 天好评率
- 车辆空间标签

## 14. 安全与风控

### 12.1 支付安全

- 回调验签
- 金额校验
- 商户号校验
- 幂等处理
- 后端签发支付参数

### 12.2 交易风控

- 高频取消订单标记
- 高频退款标记
- 可疑报价行为标记
- 服务者异常接单限制

照护闲人额外风控：

- 高频未到场或迟到用户降权
- 高频缺少反馈图片 / 视频的服务者降权
- 高频被投诉“服务不细致”“未按要求喂药”的服务者限流
- 涉及入户服务的异常投诉进入人工复核

出行司机额外风控：

- 低宠物友好分司机降权
- 高频被投诉“拒载宠物”的司机限流
- 虚假车型和空间描述触发人工审核

### 14.3 数据安全

- 用户敏感信息脱敏返回
- 定位按订单和权限控制
- 日志不得明文记录完整支付敏感参数

## 15. 代码组织建议

建议从当前 `server/src/index.ts` 单文件路由，调整为模块结构：

```text
server/src/
  index.ts
  app.ts
  config/
  modules/
    auth/
    users/
    demands/
    offers/
    orders/
    payments/
    messages/
    navigation/
    policy/
  services/
  repositories/
  middleware/
  utils/
```

`shared/` 建议新增：

```text
shared/
  index.ts
  demand.ts
  order.ts
  payment.ts
  message.ts
  location.ts
```

新增 Python 服务目录建议：

```text
python-service/
  app/
    main.py
    api/
    modules/
    services/
    repositories/
    models/
    schemas/
    workers/
    rules/
  migrations/
  tests/
```

## 16. 迁移策略

### 14.1 第一阶段

- 保留现有政策和首页功能
- 新增 Python 领域服务
- 新增 PostgreSQL
- 新增用户、需求、订单、支付、消息模块
- TS 层改造成接入层 / BFF
- 前端先接静态接口和 mock 数据

### 14.2 第二阶段

- 接支付宝
- 打通真实支付回调
- 打通导航跳转
- 打通消息轮询
- 打通 TS -> Python 同步调用链路

### 14.3 第三阶段

- 加微信支付
- 加实时消息
- 加服务评价
- 加异步画像和规则重算

## 17. MVP 实施顺序

建议按以下顺序开发：

1. 扩展 `shared` 类型定义。
2. 抽离 TS 接入层职责并定义 TS <-> Python DTO。
3. 新增 Python 服务并接入 PostgreSQL。
4. 实现登录和用户资料。
5. 实现需求发布、需求大厅、报价、选人。
6. 实现闲人资料、照护反馈和照护推荐排序。
7. 实现车辆资料、司机评价和宠物友好排序。
8. 实现订单模型和状态机。
9. 实现支付宝单渠道支付。
10. 实现消息中心。
11. 实现高德 / 百度导航跳转。
12. 前端联调和异常状态补齐。

## 18. 当前仓库需要补的内容

当前仓库中需要新增的关键能力：

- Python 领域服务工程
- PostgreSQL 持久化替代 JSON FS
- 新的共享类型
- 模块化 Express 路由
- TS <-> Python 内部 DTO 和调用层
- 支付渠道配置
- 支付回调处理
- 订单事件日志
- 消息会话模型
- 导航链接生成
- 闲人照护能力模型
- 服务过程反馈模型
- 照护推荐排序逻辑
- 车辆资料模型
- 司机多维评价模型
- 宠物友好排序逻辑

## 19. 参考接入说明

- 支付宝开放平台 Web/App 接入与应用配置：`https://open.alipay.com/module/webApp`
- 高德 URI 产品指南：`https://a.amap.com/lbs/static/file/%E6%97%A7%E7%89%88URI%E4%BA%A7%E5%93%81%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97.pdf`
- 百度地图 URI API 文档入口：`https://lbsyun.baidu.com/`
